import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import React from "react";

const Icons = ({ icon }) => {
  return <FontAwesomeIcon icon={icon} className="mx-3" />;
};

export default Icons;
